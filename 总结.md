# Code Review 总结

> 每次代码审查的结果记录在此文件中。

## #1 - 2026/05/10

**Commit**: `b3c10e2f` - feat:项目初始化以及添加了三个测试页面和布局组件（待完善）

### 错误/改进项

**1. 路由高亮用了前缀匹配 class**
- **文件**: `app/assets/css/main.css:13`
- **问题代码**:
  ```css
  .router-link-active {
    @apply font-extrabold
  }
  ```
- **错误总结**: `router-link-active` 是前缀匹配，在 `/posts` 页面时 HOME 链接（`to="/"`）也会被高亮。应使用 `router-link-exact-active` 精确匹配。
- **改进方案**:
  ```css
  .router-link-exact-active {
    @apply font-extrabold;
  }
  ```

**2. `<title>` 标签放在 template 里不起作用**
- **文件**: `app/pages/index.vue:4`、`app/pages/Posts.vue:3`
- **问题代码**:
  ```vue
  <title>Gerden Blog - Welcome</title>
  ```
- **错误总结**: Vue template 里的 `<title>` 只渲染为 DOM 元素，不会修改浏览器标签页标题。Nuxt 需要用 `useHead()` 设置。
- **改进方案**:
  ```vue
  <script lang="ts" setup>
  useHead({ title: 'Gerden Blog - Welcome' })
  </script>
  ```

**3. NavBar 缺少语义化 `<nav>` 标签，结构过于复杂**
- **文件**: `app/components/NavBar.vue:2-14`
- **问题代码**:
  ```vue
  <div class="font-serif py-3 flex text-text-primary justify-between mt-4">
    <div class="text-3xl">GDB</div>
    <div class="w-[30%]">
      <div class="flex gap-4 w-[100%]">
        <div class="w-[30%] text-center">
          <NuxtLink to="/">HOME</NuxtLink>
        </div>
        ...
  ```
- **错误总结**: 导航栏应使用 `<nav>` 语义标签（无障碍要求）；链接外层套了不必要的嵌套 div，`<div>|</div>` 分隔符应使用 `<span>`；`w-[100%]` 和 `w-[30%]` 的固定百分比布局不够灵活。
- **改进方案**:
  ```vue
  <nav class="font-serif py-3 flex text-text-primary justify-between mt-4">
    <div class="text-3xl">GDB</div>
    <div class="flex gap-4">
      <NuxtLink to="/">HOME</NuxtLink>
      <span>|</span>
      <NuxtLink to="/posts">POSTS</NuxtLink>
      <span>|</span>
      <NuxtLink to="/works">WORK</NuxtLink>
    </div>
  </nav>
  ```

**4. Prisma 环境变量使用非空断言，缺少校验**
- **文件**: `server/utils/prisma.ts:6-10`
- **问题代码**:
  ```ts
  host: process.env.DATABASE_HOST!,
  ```
- **错误总结**: `!` 非空断言在 env 缺失时不会报错，静默传入 `undefined`，运行时才抛出难以理解的错误。应在启动时给出清晰提示。
- **改进方案**:
  ```ts
  function getEnv(key: string): string {
    const value = process.env[key]
    if (!value) throw new Error(`缺少环境变量: ${key}`)
    return value
  }

  const adapter = new PrismaMariaDb({
    host: getEnv('DATABASE_HOST'),
    port: Number(process.env.DATABASE_PORT ?? 3306),
    user: getEnv('DATABASE_USER'),
    password: getEnv('DATABASE_PASSWORD'),
    database: getEnv('DATABASE_NAME'),
    connectionLimit: 5
  })
  ```

**5. tsconfig.json 自定义 compilerOptions 可能与 Nuxt 冲突**
- **文件**: `tsconfig.json:17-23`
- **问题代码**:
  ```json
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "node",
    "target": "ES2023",
    "strict": true,
    "esModuleInterop": true,
    "ignoreDeprecations": "6.0"
  }
  ```
- **错误总结**: Nuxt 自动生成 `.nuxt/tsconfig.app.json` 并通过 references 引入。手动在根 tsconfig 加 compilerOptions 可能冲突，`strict: true` 可能导致 Nuxt 生成的类型报错。
- **改进方案**: 删除手动添加的 `compilerOptions`，需要自定义时在 `nuxt.config.ts` 的 `typescript` 选项中设置。

**6. 多个文件末尾缺少换行符**
- **文件**: `main.css`、`NavBar.vue`、`PostListContainer.vue`、`Posts.vue`、`Works.vue`、`index.vue`、`prisma.ts`、`tsconfig.json`
- **错误总结**: 文件末尾无换行符，不符合 POSIX 标准，也会在 diff 中产生噪音。

**7. 空 `<style>` 块和多余前导空格**
- **文件**: 多个 `.vue` 组件
- **错误总结**: `PostListContainer.vue`、`Posts.vue`、`Works.vue`、`index.vue` 有空的 `<style>` 块；`app.vue:2` 的 class 属性有前导空格。不需要的块应删除。

---

## #2 - 2026/05/14

**Commit**: `82d0383` - feat:封装了全局API实例，并完成文章请求模块的封装，实现了文章列表请求方法getList

### 错误/改进项

**1. 返回值用了死代码，逻辑错误**
- **文件**: `server/api/posts/index.get.ts:31-39`
- **问题代码**:
  ```ts
  const result: PostList[] = posts.map((item) => ({
    id: item.id.toString(),
    title: item.title,
    // ...
  }))
  return success<PostList[]>(serializeBigInt<typeof posts, PostList[]>(posts))
  ```
- **错误总结**: 第31行用 `.map()` 做了字段映射得到 `result`，但第39行返回的仍然是 `serializeBigInt(posts)` 而不是 `result`。`result` 是死代码，字段映射完全没有生效。
- **改进方案**:
  ```ts
  const result: PostList[] = posts.map((item) => ({
    id: item.id.toString(),
    title: item.title,
    summary: item.summary ?? '',
    published_at: item.published_at!,
    tags: item.post_tags.map((pt) => (pt.tags.name))
  }))
  return success<PostList[]>(result)
  ```
  同时移除 `serializeBigInt` 的 import。

**2. `useAPI` 泛型类型错误**
- **文件**: `app/services/posts.ts:4`
- **问题代码**:
  ```ts
  return useAPI<PostList>('posts')
  ```
- **错误总结**: 后端接口返回的是 `APIResponse<PostList[]>`（包了 status/message/data 的结构），泛型缺少数组 `[]`，也没有包裹 `APIResponse`。
- **改进方案**:
  ```ts
  return useAPI<APIResponse<PostList[]>>('posts')
  ```

**3. `PostList.published_at` 类型应为 `string`**
- **文件**: `app/types/posts.ts:21`
- **问题代码**:
  ```ts
  published_at: Date,
  ```
- **错误总结**: `Date` 对象经过 JSON 序列化后变成 ISO 字符串，前端实际拿到的是 `string`，类型声明与运行时不一致。
- **改进方案**:
  ```ts
  published_at: string,
  ```

**4. 变量名遮蔽（shadowing）**
- **文件**: `server/api/posts/index.get.ts:36`
- **问题代码**:
  ```ts
  tags: item.post_tags.map((item) => (item.tags.name))
  ```
- **错误总结**: 内层 `item` 遮蔽了外层 `.map()` 的 `item`，影响可读性。
- **改进方案**:
  ```ts
  tags: item.post_tags.map((pt) => (pt.tags.name))
  ```

**5. 401 错误会打印两次日志，且用 `console.log` 而非 `console.error`**
- **文件**: `app/plugins/api.ts:10-14`
- **问题代码**:
  ```ts
  onResponseError({ response }) {
    if (response.status === 401) {
      console.log('响应错误！...')
    }
    console.log('响应错误！错误代码:', response.status, ' 错误内容:', response._data)
  }
  ```
- **错误总结**: 401 时会打印两条日志；错误场景应用 `console.error`。
- **改进方案**:
  ```ts
  onResponseError({ response }) {
    console.error('响应错误！错误代码:', response.status, '错误内容:', response._data)
  }
  ```

**6. 残留调试日志**
- **文件**: `server/utils/prisma.ts:16`
- **问题代码**:
  ```ts
  console.log('prisma client setup success')
  ```
- **错误总结**: 调试日志不应留在生产代码中。

**7. `createUseFetch` 不是 Nuxt 标准 API**
- **文件**: `app/composables/useAPI.ts:1`
- **错误总结**: `createUseFetch` 不是 Nuxt 内置函数，建议确认来源或改用标准的 `useFetch` 封装方式。

**8. `coverImage` 字段在 select 和 map 中都遗漏了**
- **文件**: `server/api/posts/index.get.ts`
- **错误总结**: `PostList` 接口定义了 `coverImage?: string`，但 Prisma 查询的 `select` 没有包含 `cover_image`，`.map()` 也没有映射。因为是可选字段所以 TS 不报错，但数据会丢失。

**9. 多个新增文件缺少末尾换行符**
- **文件**: `useAPI.ts`、`api.ts`、`posts.ts`、`response.ts`、`serializeBigInt.ts`、`index.get.ts`、`tsconfig.json` 等
- **错误总结**: 文件末尾无换行符，不符合 POSIX 标准。

---

## #3 - 2026/05/14

**Commit**: `6215148` - feat:初步实现了文章列表页面的对接

### 错误/改进项

**1. `serializeBigInt` 和 `error` 变成死导入**
- **文件**: `server/api/posts/index.get.ts:2-3`
- **问题代码**:
  ```ts
  import serializeBigInt from "~~/server/utils/api/serializeBigInt"
  import { success, error } from "~~/server/utils/api/response"
  ```
- **错误总结**: 返回值已改为 `success<PostList[]>(result)`，`serializeBigInt` 和 `error` 不再使用，属于死导入。
- **改进方案**: 删除这两行 import。

**2. 变量名遮蔽仍未修复（review #2 遗留）**
- **文件**: `server/api/posts/index.get.ts:36`
- **问题代码**:
  ```ts
  tags: item.post_tags.map((item) => (item.tags.name))
  ```
- **错误总结**: 内层 `item` 遮蔽外层 `item`，影响可读性。
- **改进方案**:
  ```ts
  tags: item.post_tags.map((pt) => (pt.tags.name))
  ```

**3. Posts.vue 有多个无用导入**
- **文件**: `app/pages/Posts.vue:17-19`
- **问题代码**:
  ```ts
  import { ref , computed} from 'vue';
  import type { PostList } from '~/types/posts';
  ```
- **错误总结**: `ref` 未使用；`PostList` 类型未使用；Nuxt 会 auto-import `ref`/`computed`，手动 import 冗余。
- **改进方案**: 删除这三行 import。

**4. 解构了 `isLoading`/`status` 但未在模板中使用**
- **文件**: `app/pages/Posts.vue:21`
- **问题代码**:
  ```ts
  const {data, pending:isLoading, status} = await PostApi.getList()
  ```
- **错误总结**: 请求中和出错时用户无任何反馈，应在 template 中展示 loading/error 状态。
- **改进方案**:
  ```vue
  <div v-if="isLoading">加载中...</div>
  <div v-else-if="status === 'error'">加载失败</div>
  <div v-else>
    <!-- 文章列表 -->
  </div>
  ```

**5. `hover:text-accent` 挂在非交互元素上**
- **文件**: `app/components/posts/PostListContainer.vue:3`
- **问题代码**:
  ```html
  <h1 class="text-3xl font-extrabold hover:text-accent">{{ props.posts.title }}</h1>
  ```
- **错误总结**: hover 变色暗示可点击，但 `<h1>` 无链接/事件，误导用户。
- **改进方案**: 如果标题要跳转文章详情，用 `<NuxtLink>` 包裹；否则去掉 `hover:text-accent`。

**6. 注释掉的调试代码**
- **文件**: `app/pages/Posts.vue:23`
- **问题代码**: `// console.log(postList.value)`
- **错误总结**: 调试代码应直接删除，不应注释保留。

**7. `summary??''` 缺少空格且不必要**
- **文件**: `app/components/posts/PostListContainer.vue:4`
- **问题代码**:
  ```html
  <h2 class="text-xl">{{ props.posts.summary??'' }}</h2>
  ```
- **错误总结**: `??` 前缺空格（格式问题）；Vue 模板对 `undefined` 渲染为空字符串，`??''` 冗余。
- **改进方案**:
  ```html
  <h2 class="text-xl">{{ props.posts.summary }}</h2>
  ```

**8. `<title>` 仍在 template 中（review #1 遗留）**
- **文件**: `app/pages/Posts.vue:3`
- **错误总结**: 上一轮已指出，仍未修复。Nuxt 需用 `useHead()` 设置页面标题。

**9. 空 `<style>` 块**
- **文件**: `PostListContainer.vue:16-17`、`Posts.vue:28-30`
- **错误总结**: 不需要的空 style 块应删除。

**10. `published_at.slice(0, 10)` 日期格式化较脆弱**
- **文件**: `app/components/posts/PostListContainer.vue:5`
- **问题代码**:
  ```html
  <div>{{ props.posts.published_at.slice(0, 10) }}</div>
  ```
- **错误总结**: 硬切 ISO 字符串可工作但不够健壮，且显示格式固定为 `YYYY-MM-DD`。
- **改进方案**: 后续可提取为日期格式化函数或使用 `Intl.DateTimeFormat`。

**11. `services/posts.ts` 中 `Posts` 类型未使用**
- **文件**: `app/services/posts.ts:3`
- **问题代码**:
  ```ts
  import type { PostList, Posts } from "~/types/posts";
  ```
- **错误总结**: `Posts` 类型未使用，应移除。

---

## #4 - 2026/05/27

**Commit**: `cef42d5` - feat:实现表单验证、登录错误拦截、登录成功识别

### 错误/改进项

**1. 登录成功后没有跳转逻辑**
- **文件**: `app/pages/admin/login.vue:92-94`
- **问题代码**:
  ```ts
  else {
    logSuccess.value = true  
  }
  ```
- **错误总结**: 设置了 logSuccess 显示 "Redirecting..." 但没有 navigateTo 调用，用户会卡在这个状态
- **改进方案**:
  ```ts
  else {
    logSuccess.value = true
    await navigateTo('/admin')
  }
  ```

**2. 重复提交时 logError 没有重置**
- **文件**: `app/pages/admin/login.vue:79-94`
- **问题代码**:
  ```ts
  const handleSubmit = async () => {
    isSubmiting.value = true
    validatePsw()
    // ...
  ```
- **错误总结**: 第一次登录失败后 logError 为 true，再次提交时旧错误信息仍残留
- **改进方案**:
  ```ts
  const handleSubmit = async () => {
    logError.value = false
    logMsg.value = ''
    isSubmiting.value = true
    // ...
  ```

**3. 可选链不完整，可能报运行时错误**
- **文件**: `app/pages/admin/login.vue:83`
- **问题代码**:
  ```ts
  logMsg.value = error.value?.data.message ?? 'Login fail'
  ```
- **错误总结**: `data` 本身可能是 undefined，需要 `data?.message`
- **改进方案**:
  ```ts
  logMsg.value = error.value?.data?.message ?? 'Login fail'
  ```

**4. isSubmiting 拼写错误**
- **文件**: `app/pages/admin/login.vue:54`
- **问题代码**:
  ```ts
  const isSubmiting = ref<boolean>(false)
  ```
- **错误总结**: Submitting 有两个 t，应为 isSubmitting
- **改进方案**:
  ```ts
  const isSubmitting = ref<boolean>(false)
  ```

**5. error 函数导入但未使用**
- **文件**: `server/api/admin/login.post.ts:1`
- **问题代码**:
  ```ts
  import { success, error } from '../../utils/api/response'
  ```
- **错误总结**: 服务器端用 createError 处理错误，error 未使用
- **改进方案**:
  ```ts
  import { success } from '../../utils/api/response'
  ```

**6. layout 文件缺少末尾换行**
- **文件**: `app/layouts/blank.vue`, `app/layouts/default.vue`
- **错误总结**: 文件末尾没有换行符，部分工具可能处理异常
- **改进方案**: 在文件末尾添加空行

**7. 表单缺少 Enter 键提交**
- **文件**: `app/pages/admin/login.vue:4`
- **问题代码**:
  ```html
  <form class="text-xl mt-8" v-if="!logSuccess">
  ```
- **错误总结**: 用户在输入框按 Enter 无法提交，需要在 form 上加 @submit.prevent
- **改进方案**:
  ```html
  <form class="text-xl mt-8" v-if="!logSuccess" @submit.prevent="handleSubmit">
  ```

**8. 两个 layout 重复硬编码 max-w-[47%]**
- **文件**: `app/layouts/blank.vue`, `app/layouts/default.vue`
- **错误总结**: 相同的宽度值在两个 layout 中硬编码，未来修改需要改两处
- **改进方案**: 提取到 Tailwind 配置或 CSS 变量中统一管理

**9. as object 类型断言丢失类型信息**
- **文件**: `server/services/post.service.ts:144`
- **问题代码**:
  ```ts
  toc: JSON.parse(JSON.stringify(toc)) as object,
  ```
- **错误总结**: `as object` 过于宽泛，丢失了类型信息
- **改进方案**:
  ```ts
  toc: JSON.parse(JSON.stringify(toc)) as Prisma.InputJsonValue,
  ```

---
