# 问答

## Token 的签发和验证过程

### 问题

我不太记得 token 的签发和验证的过程了，你结合代码帮我回顾一下？

### 回答

token 可以分成两段流程理解：

```txt
登录时：签发 token
访问受保护接口时：验证 token
```

#### 1. 登录时签发 token

在 `/api/login` 中，后端先做登录校验：

```ts
const user = await findUserByUsername(db, username)
await verifyPassword(password, user.passwordHash)
```

这一步确认：

```txt
用户名存在
密码正确
用户状态 active
```

然后创建一次登录会话：

```ts
const sessionId = crypto.randomUUID()
```

这个 `sessionId` 会同时存在于两个地方：

```txt
sessions 表里的 id
JWT payload 里的 sid
```

之后签发 access token：

```ts
const accessToken = await createAccessToken({
  userId: user.id,
  sessionId,
  role: user.role,
  tokenVersion: user.tokenVersion
}, c.env.JWT_SECRET)
```

`createAccessToken` 本质上是在做：

```ts
sign({
  sub: userId,
  sid: sessionId,
  role,
  tokenVersion,
  iat: now,
  exp: now + 15分钟
}, JWT_SECRET, 'HS256')
```

payload 字段含义：

| 字段 | 作用 |
|---|---|
| `sub` | user id |
| `sid` | 当前登录 session id |
| `role` | 用户角色 |
| `tokenVersion` | 全局 token 失效版本号 |
| `iat` | 签发时间 |
| `exp` | 过期时间 |

最后把 token 写入 HttpOnly cookie：

```ts
setAuthCookies(c, accessToken, refreshToken)
```

浏览器后续访问 API 时会自动带上 cookie，前端 JS 不需要也不应该读取 token。

#### 2. `sign` 做了什么

JWT 不是加密，而是签名。

它的结构大概是：

```txt
header.payload.signature
```

可以理解为：

```txt
header: { alg: "HS256", typ: "JWT" }
payload: { sub, sid, role, tokenVersion, iat, exp }
signature: HMAC(header + payload, JWT_SECRET)
```

所以 JWT payload 可以被解码看到，但不能被伪造。因为没有 `JWT_SECRET`，就无法算出正确的签名。

#### 3. 访问 `/api/me` 时验证 token

`/api/me` 会挂认证 middleware：

```ts
authRoutes.get('/me', requireAuth, async (c) => {
  // ...
})
```

`requireAuth` 首先从 cookie 读取 token：

```ts
const token = getCookie(c, ACCESS_COOKIE)
```

然后验证 token：

```ts
const payload = await verifyAccessToken(token, c.env.JWT_SECRET)
```

`verifyAccessToken` 内部核心是：

```ts
verify(token, JWT_SECRET, 'HS256')
```

这一步会检查：

```txt
签名是否正确
token 是否过期
算法是否匹配
```

如果 token 被篡改、过期，或者 secret 不匹配，验证都会失败。

#### 4. 为什么验证 JWT 后还要查数据库

JWT 验证成功，只能说明：

```txt
这个 token 是后端签过的
这个 token 还没有过期
```

但它不能说明：

```txt
用户现在仍然存在
用户状态仍然 active
这个 session 没有被 logout
这个用户的所有旧 token 没有被全局失效
```

所以 `requireAuth` 还要继续查数据库：

```ts
const user = await findUserById(db, payload.sub)
const session = await findActiveSessionById(db, payload.sid)
```

然后检查：

```txt
user 是否存在
user.status 是否 active
user.tokenVersion 是否等于 payload.tokenVersion
session 是否 active
session 是否没过期
```

全部通过后，才把当前用户挂到 Hono 上下文中：

```ts
c.set('user', {
  id: user.id,
  sessionId: payload.sid,
  role: payload.role,
  tokenVersion: user.tokenVersion
})
```

后面的 route 就可以通过下面方式读取当前用户：

```ts
const user = c.get('user')
```

#### 5. logout 为什么能让 token 失效

普通 logout 不需要修改 `users.tokenVersion`，只需要撤销当前 session：

```ts
await revokeSession(db, payload.sid)
```

即使浏览器还带着旧的 `access_token`，下一次经过 `requireAuth` 时，后端会查到：

```txt
sid 对应的 session 已经不是 active
```

于是请求会返回 401。

#### 总结

```txt
login:
校验密码 -> 创建 session -> 把 userId/sessionId/role/tokenVersion 签进 JWT -> 写 cookie

requireAuth:
读 cookie -> 验 JWT 签名和过期时间 -> 查 user -> 查 session -> 放入 c.set('user')
```

JWT 负责证明“这个 token 是不是后端签的”，`sessions` 表负责判断“这次登录现在还算不算有效”。
