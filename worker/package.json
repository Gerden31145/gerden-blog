{
  "name": "worker",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply gerden-blog-db --local"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.25"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260611.1",
    "drizzle-kit": "^0.31.10",
    "wrangler": "^4.4.0"
  }
}